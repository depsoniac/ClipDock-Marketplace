# ClipDock Marketplace

Sube estos archivos a la raíz de un repo público llamado `ClipDock-Marketplace` y activa GitHub Pages desde `main` / `/root`.

URL del catálogo para pegar en ClipDock:

```txt
https://depsoniac.github.io/ClipDock-Marketplace/catalog.json
```

Estructura esperada:

```txt
index.html
catalog.json
plugins/clipdock-remote-adobe/clipdock-remote-adobe-1.0.0.zip
```

SHA256 del complemento:

```txt
d81777be6701c3eda7fa2eb30751be5b6e9a597963a0798fd138cf8eb6228daa
```

Si cambias el nombre del repo o usuario, edita en `catalog.json` el campo `downloadUrl` y en `index.html` la URL mostrada.
